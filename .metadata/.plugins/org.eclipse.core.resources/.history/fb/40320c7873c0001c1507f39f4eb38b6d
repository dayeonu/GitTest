import java.util.Scanner;

public class 함수문제 {

	public static void main(String[] args) {

		Scanner sc = new Scanner(System.in);
		int age = sc.nextInt();
		double feeCalcRate = sc.nextInt();
		/*		
			메인함수
			input함수를 통해 나이를 입력 받아야함.
			feeCalcRate 함수를 통해 할인율 계산을 받아야함.
			output 함수를 통해 요금을 계산 

		 */	

		double num = add((int)age,(double)feeCalcRate);
		System.out.println(num);
	}

	static double add(int i, double j ) { //반환타입(자료형)도 동일해야함, int i, int j는 매개변수
		return i*j; // 리턴할때, num에게 값이 넘어감 	
	}
}